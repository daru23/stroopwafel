# Copy to .env and fill in values before running docker compose.

# Secret token the app uses to authenticate API requests.
# Generate one with: openssl rand -hex 32
API_TOKEN=

# Host port for the web container (default 8080).
# HOST_PORT=8080
